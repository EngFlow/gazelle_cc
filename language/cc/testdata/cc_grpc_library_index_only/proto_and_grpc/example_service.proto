syntax = "proto3";

package proto_and_grpc;

import "google/protobuf/empty.proto";

message Message {
    string content = 1;
}

service ExampleService {
    rpc Call(Message) returns (google.protobuf.Empty);
}
